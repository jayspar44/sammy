PORT=4001
FIREBASE_SERVICE_ACCOUNT={"type": "service_account", ...}
GEMINI_API_KEY=your-gemini-api-key
NODE_ENV=development
# Comma-separated list of allowed origins for CORS
# Local dev doesn't need this (defaults to localhost:4000, localhost:5173, capacitor://localhost)
# For deployed environments, set to your Firebase Hosting + capacitor URLs:
# ALLOWED_ORIGINS=https://sammy-658-dev.web.app,https://sammy-658.web.app,capacitor://localhost
